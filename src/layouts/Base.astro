---
import { meta, site } from '~/config/site';
import '~/styles/nocturne.css';
import '~/styles/page.css';

interface Props {
  title?: string;
  description?: string;
}

const { title = meta.title, description = meta.description } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site);
// Le piattaforme di condivisione vogliono un URL assoluto per l'immagine.
const ogImage = site.ogImage.trim() ? new URL(site.ogImage.trim(), Astro.site) : null;
---

<!doctype html>
<html lang={meta.lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="canonical" href={canonical} />
    <title>{title}</title>
    <meta name="description" content={description} />

    <meta property="og:type" content="website" />
    <meta property="og:locale" content={meta.locale} />
    <meta property="og:title" content={meta.ogTitle} />
    <meta property="og:description" content={meta.ogDescription} />
    <meta property="og:site_name" content="Discerns" />
    <meta property="og:url" content={canonical} />
    <meta name="twitter:card" content="summary_large_image" />
    {ogImage && <meta property="og:image" content={ogImage} />}
    {ogImage && <meta property="og:image:width" content="1200" />}
    {ogImage && <meta property="og:image:height" content="630" />}
    {ogImage && <meta property="og:image:alt" content={meta.ogTitle} />}
    {ogImage && <meta name="twitter:image" content={ogImage} />}

    <meta name="theme-color" content="#161826" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <!-- Inter corsivo 600: solo per le parole in accento del titolo del hero. -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@1,600&display=swap" />
  </head>
  <body>
    <a class="skip-link" href="#top">Vai al contenuto</a>
    <slot />

    <script>
      // Le animazioni esistono solo se il browser sa osservare l'intersezione.
      // Senza questo attributo il CSS lascia tutto visibile, quindi un
      // fallimento qui degrada a pagina statica, non a pagina vuota.
      if ('IntersectionObserver' in window) {
        document.body.setAttribute('data-anim', '');

        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('in');
                io.unobserve(entry.target);
              }
            });
          },
          { threshold: 0.15, rootMargin: '0px 0px -8% 0px' }
        );

        document
          .querySelectorAll('.reveal:not(.in), .step-row:not(.in)')
          .forEach((el) => io.observe(el));
      }
    </script>
  </body>
</html>
